Here is the README in **pure copy-paste format** (no explanations, no extra text):

---

# 🚀 mini HTTP Server with DNS Resolver


## 📌 Features

* Custom DNS server (resolves domain → `127.0.0.1`)
* Minimal HTTP server in Python
* Supports GET 
* Test with your own local domain (e.g., `nwtest.ir`)

## 🛠 Requirements

* Python 3.8+
* Ports required:

  * 80 (HTTP)
  * 53 (DNS)

## ▶️ How to Run

### 1. Change your DNS to `127.0.0.1`

Set your system DNS to your local machine.

### 2. Run the HTTP server

```bash
python http_server.py
```

### 3. Run the DNS server

```bash
python DNS_server.py
```

### 4. Open your browser

Go to:

```
http://nwtest.ir
```

## 📂 Repository

[https://github.com/MohammadKazemi-2004/mini-HTTP-server-with-DNS-resolver](https://github.com/MohammadKazemi-2004/mini-HTTP-server-with-DNS-resolver)

